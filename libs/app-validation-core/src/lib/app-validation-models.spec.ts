import { appValidationModels } from './app-validation-models'

describe('appValidationModels', () => {
    it('should work', () => {
        expect(appValidationModels()).toEqual('app-validation-models')
    })
})
